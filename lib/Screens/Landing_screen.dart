import 'package:flutter/material.dart';

class Landing extends StatelessWidget {
  const Landing({super.key});
  static const land_route = '/landing';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}